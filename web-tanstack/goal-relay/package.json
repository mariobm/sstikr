{
  "name": "sstikr-goal-relay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "cf-typegen": "wrangler types --config wrangler.jsonc",
    "pretypecheck": "wrangler types --config wrangler.jsonc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy:dry-run": "wrangler deploy --config wrangler.jsonc --dry-run",
    "deploy": "wrangler deploy --config wrangler.jsonc"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.4",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  }
}
