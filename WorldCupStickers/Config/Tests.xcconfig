// Tests.xcconfig
// Test configuration for iOS projects - minimal overrides only
// Generated by XcodeBuildMCP

#include "Shared.xcconfig"

// The UI-test bundle only launches the host app. Do not inherit the host app's
// APNs or associated-domain capabilities, which its provisioning profile owns.
CODE_SIGN_ENTITLEMENTS =

// ==========================================
// Test Target Settings (Customizable by scaffold tool)
// ==========================================
PRODUCT_BUNDLE_IDENTIFIER = com.sstikr.worldcupstickers.uitests
TEST_TARGET_NAME = WorldCupStickers

// Fix duplicate module name issue
PRODUCT_MODULE_NAME = $(PRODUCT_NAME)UITests
